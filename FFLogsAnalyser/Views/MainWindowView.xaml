<UserControl x:Class="FFLogsAnalyser.Views.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FFLogsAnalyser.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1040">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 1 -->

        <TabControl x:Name="Items" Grid.Row="1" Grid.Column="1" Height="200" Width="1000" Background="Transparent"/>

        <!-- Row 2 -->

        <DataGrid x:Name="SelectedParses" Grid.Row="2" Grid.Column="1">
        </DataGrid>
        
        <!-- Row 3 -->
        <ScrollViewer Grid.Row="3" Grid.Column="1">
            <ContentControl x:Name="TimeLineBaseViewModel" HorizontalContentAlignment="Left" VerticalContentAlignment="Top" Margin="0,10,0,0"/>
        </ScrollViewer>


    </Grid>
</UserControl>
